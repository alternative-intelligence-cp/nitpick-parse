# `examples/`

Runnable demonstrations, **built and run by the harness**, so a broken example
is a red run. One per format plus the architectural ones — a streaming
consumer, a custom `Format` plugin, and the cycle-0.13 dogfood program.
